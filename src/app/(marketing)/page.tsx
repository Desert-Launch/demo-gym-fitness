import { PageContainer } from "@/components/layout/page-container"

// Placeholder home page — replaced by the real marketing build in a later commit.
export default function HomePage() {
  return (
    <PageContainer className="py-section">
      <p className="eyebrow text-brand">01 — Forge</p>
      <h1 className="display mt-6 text-display-xl">Train like it counts</h1>
    </PageContainer>
  )
}
